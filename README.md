# python-def-function

  this repository file contains my activity about def function.

# About Files
 
 # def_ activity.py 
 – a program that defines a function called `calculate_average()`, 
   # calculate_ average()
   –  invokes and prints the average scores.
   
# README.md 
– contains basic information about the def_ activity.py repository file.

# What is a def function?
A `def` function help us reuse the code we wrote inside a function and can invoke or call it many times,
without writing it again (or repeatedly). It helps the code have a more organize outline. 
